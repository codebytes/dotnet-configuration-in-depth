## Overview

- Binds complex collections (lists, dictionaries, primitive arrays).
- Enforces required members and DataAnnotations with ValidateOnStart.

## Run

```powershell
dotnet run --project 26-collections-binding/CollectionsBinding.csproj
```

## Notes

- Use env vars with the COLLECTIONS_ prefix to override specific values.
