## Overview

- Contains configuration-focused unit tests (binding, options, validation).
- Shows when to use in-memory providers versus mocks.

## Run

```powershell
dotnet test 28-configuration-testing/ConfigurationTesting.csproj
```

## Notes

- Run directly from the repo root; no extra test settings are required.
