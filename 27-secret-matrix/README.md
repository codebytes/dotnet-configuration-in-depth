## Overview

- Compares JSON, user secrets, KeyPerFile, environment variables, and CLI overrides.
- Displays precedence ordering for secret storage strategies.

## Run

```powershell
dotnet run --project 27-secret-matrix/SecretMatrix.csproj
```

## Notes

- Optional: init user secrets and create a secrets/ folder to experiment with each strategy.
