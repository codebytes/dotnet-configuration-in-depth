## Overview

- Demonstrates options validation with DataAnnotations and custom validators.
- Uses ValidateOnStart to fail fast when settings are invalid.

## Run

```powershell
dotnet run --project 23-options-validation/OptionsValidation.csproj
```

## Notes

- Edit appsettings.json or provide environment variables to observe validation failures.
