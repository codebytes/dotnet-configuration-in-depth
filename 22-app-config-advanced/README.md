## Overview

- Extends the App Configuration sample with feature flags and debug endpoints.
- Provides routes to inspect values, refresh state, and feature toggles.

## Run

```powershell
dotnet run --project 22-app-config-advanced/AppConfigAdvanced.csproj
```

## Notes

- Reuse the App Configuration endpoint secret and pre-create feature flags to exercise the sample.
